// pair function
// takes in 2 arguments and returns them in an array

const pear = (a,b) => [a,b]

pear('dog', 'cat')

// expected ['dog', 'cat']

//	19/11 :48

const pair3 = (a, b) => [a, b]

pair3('mike', 'ximing') // => ['mike', 'ximing']

